---
type: qa-note
category: Claude대화
domain: 기타
date: 2026-05-24
status: verified
tags:
  - QA
  - 기타
---

# QA - 여기서 어디로 가야되?

## 질문
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

여기서 어디로 가야되?

## 답변

현재 화면에서 순서대로 따라오세요.

**1단계 — Custom Search API 활성화**

왼쪽 메뉴 (≡) → **API 및 서비스** → **라이브러리** → 검색창에 `Custom Search API` 입력 → 클릭 → **사용 설정** 버튼

---

**2단계 — API 키 생성**

왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보** → 상단 **+ 사용자 인증 정보 만들기** → **API 키** 선택 → 생성된 키 복사

---

왼쪽 메뉴가 안 보이면 화면 왼쪽 상단 **≡ (햄버거 메뉴)** 클릭하면 나옵니다.

## 관련 자료

- [[MOC - Q&A Knowledge Base]]

## 연결

- [[Global Knowledge Map]]

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

여기서 어디로 가야되?

## 답변

현재 화면에서 순서대로 따라오세요.

**1단계 — Custom Search API 활성화**

왼쪽 메뉴 (≡) → **API 및 서비스** → **라이브러리** → 검색창에 `Custom Search API` 입력 → 클릭 → **사용 설정** 버튼

---

**2단계 — API 키 생성**

왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보** → 상단 **+ 사용자 인증 정보 만들기** → **API 키** 선택 → 생성된 키 복사

---

왼쪽 메뉴가 안 보이면 화면 왼쪽 상단 **≡ (햄버거 메뉴)** 클릭하면 나옵니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

여기서 어디로 가야되?

## 답변

현재 화면에서 순서대로 따라오세요.

**1단계 — Custom Search API 활성화**

왼쪽 메뉴 (≡) → **API 및 서비스** → **라이브러리** → 검색창에 `Custom Search API` 입력 → 클릭 → **사용 설정** 버튼

---

**2단계 — API 키 생성**

왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보** → 상단 **+ 사용자 인증 정보 만들기** → **API 키** 선택 → 생성된 키 복사

---

왼쪽 메뉴가 안 보이면 화면 왼쪽 상단 **≡ (햄버거 메뉴)** 클릭하면 나옵니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

여기서 어디로 가야되?

## 답변

현재 화면에서 순서대로 따라오세요.

**1단계 — Custom Search API 활성화**

왼쪽 메뉴 (≡) → **API 및 서비스** → **라이브러리** → 검색창에 `Custom Search API` 입력 → 클릭 → **사용 설정** 버튼

---

**2단계 — API 키 생성**

왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보** → 상단 **+ 사용자 인증 정보 만들기** → **API 키** 선택 → 생성된 키 복사

---

왼쪽 메뉴가 안 보이면 화면 왼쪽 상단 **≡ (햄버거 메뉴)** 클릭하면 나옵니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

여기서 어디로 가야되?

## 답변

현재 화면에서 순서대로 따라오세요.

**1단계 — Custom Search API 활성화**

왼쪽 메뉴 (≡) → **API 및 서비스** → **라이브러리** → 검색창에 `Custom Search API` 입력 → 클릭 → **사용 설정** 버튼

---

**2단계 — API 키 생성**

왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보** → 상단 **+ 사용자 인증 정보 만들기** → **API 키** 선택 → 생성된 키 복사

---

왼쪽 메뉴가 안 보이면 화면 왼쪽 상단 **≡ (햄버거 메뉴)** 클릭하면 나옵니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

여기서 어디로 가야되?

## 답변

현재 화면에서 순서대로 따라오세요.

**1단계 — Custom Search API 활성화**

왼쪽 메뉴 (≡) → **API 및 서비스** → **라이브러리** → 검색창에 `Custom Search API` 입력 → 클릭 → **사용 설정** 버튼

---

**2단계 — API 키 생성**

왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보** → 상단 **+ 사용자 인증 정보 만들기** → **API 키** 선택 → 생성된 키 복사

---

왼쪽 메뉴가 안 보이면 화면 왼쪽 상단 **≡ (햄버거 메뉴)** 클릭하면 나옵니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

여기서 어디로 가야되?

## 답변

현재 화면에서 순서대로 따라오세요.

**1단계 — Custom Search API 활성화**

왼쪽 메뉴 (≡) → **API 및 서비스** → **라이브러리** → 검색창에 `Custom Search API` 입력 → 클릭 → **사용 설정** 버튼

---

**2단계 — API 키 생성**

왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보** → 상단 **+ 사용자 인증 정보 만들기** → **API 키** 선택 → 생성된 키 복사

---

왼쪽 메뉴가 안 보이면 화면 왼쪽 상단 **≡ (햄버거 메뉴)** 클릭하면 나옵니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

여기서 어디로 가야되?

## 답변

현재 화면에서 순서대로 따라오세요.

**1단계 — Custom Search API 활성화**

왼쪽 메뉴 (≡) → **API 및 서비스** → **라이브러리** → 검색창에 `Custom Search API` 입력 → 클릭 → **사용 설정** 버튼

---

**2단계 — API 키 생성**

왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보** → 상단 **+ 사용자 인증 정보 만들기** → **API 키** 선택 → 생성된 키 복사

---

왼쪽 메뉴가 안 보이면 화면 왼쪽 상단 **≡ (햄버거 메뉴)** 클릭하면 나옵니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

여기서 어디로 가야되?

## 답변

현재 화면에서 순서대로 따라오세요.

**1단계 — Custom Search API 활성화**

왼쪽 메뉴 (≡) → **API 및 서비스** → **라이브러리** → 검색창에 `Custom Search API` 입력 → 클릭 → **사용 설정** 버튼

---

**2단계 — API 키 생성**

왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보** → 상단 **+ 사용자 인증 정보 만들기** → **API 키** 선택 → 생성된 키 복사

---

왼쪽 메뉴가 안 보이면 화면 왼쪽 상단 **≡ (햄버거 메뉴)** 클릭하면 나옵니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

여기서 어디로 가야되?

## 답변

현재 화면에서 순서대로 따라오세요.

**1단계 — Custom Search API 활성화**

왼쪽 메뉴 (≡) → **API 및 서비스** → **라이브러리** → 검색창에 `Custom Search API` 입력 → 클릭 → **사용 설정** 버튼

---

**2단계 — API 키 생성**

왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보** → 상단 **+ 사용자 인증 정보 만들기** → **API 키** 선택 → 생성된 키 복사

---

왼쪽 메뉴가 안 보이면 화면 왼쪽 상단 **≡ (햄버거 메뉴)** 클릭하면 나옵니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

여기서 어디로 가야되?

## 답변

현재 화면에서 순서대로 따라오세요.

**1단계 — Custom Search API 활성화**

왼쪽 메뉴 (≡) → **API 및 서비스** → **라이브러리** → 검색창에 `Custom Search API` 입력 → 클릭 → **사용 설정** 버튼

---

**2단계 — API 키 생성**

왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보** → 상단 **+ 사용자 인증 정보 만들기** → **API 키** 선택 → 생성된 키 복사

---

왼쪽 메뉴가 안 보이면 화면 왼쪽 상단 **≡ (햄버거 메뉴)** 클릭하면 나옵니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

여기서 어디로 가야되?

## 답변

현재 화면에서 순서대로 따라오세요.

**1단계 — Custom Search API 활성화**

왼쪽 메뉴 (≡) → **API 및 서비스** → **라이브러리** → 검색창에 `Custom Search API` 입력 → 클릭 → **사용 설정** 버튼

---

**2단계 — API 키 생성**

왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보** → 상단 **+ 사용자 인증 정보 만들기** → **API 키** 선택 → 생성된 키 복사

---

왼쪽 메뉴가 안 보이면 화면 왼쪽 상단 **≡ (햄버거 메뉴)** 클릭하면 나옵니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

여기서 어디로 가야되?

## 답변

현재 화면에서 순서대로 따라오세요.

**1단계 — Custom Search API 활성화**

왼쪽 메뉴 (≡) → **API 및 서비스** → **라이브러리** → 검색창에 `Custom Search API` 입력 → 클릭 → **사용 설정** 버튼

---

**2단계 — API 키 생성**

왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보** → 상단 **+ 사용자 인증 정보 만들기** → **API 키** 선택 → 생성된 키 복사

---

왼쪽 메뉴가 안 보이면 화면 왼쪽 상단 **≡ (햄버거 메뉴)** 클릭하면 나옵니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

여기서 어디로 가야되?

## 답변

현재 화면에서 순서대로 따라오세요.

**1단계 — Custom Search API 활성화**

왼쪽 메뉴 (≡) → **API 및 서비스** → **라이브러리** → 검색창에 `Custom Search API` 입력 → 클릭 → **사용 설정** 버튼

---

**2단계 — API 키 생성**

왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보** → 상단 **+ 사용자 인증 정보 만들기** → **API 키** 선택 → 생성된 키 복사

---

왼쪽 메뉴가 안 보이면 화면 왼쪽 상단 **≡ (햄버거 메뉴)** 클릭하면 나옵니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

여기서 어디로 가야되?

## 답변

현재 화면에서 순서대로 따라오세요.

**1단계 — Custom Search API 활성화**

왼쪽 메뉴 (≡) → **API 및 서비스** → **라이브러리** → 검색창에 `Custom Search API` 입력 → 클릭 → **사용 설정** 버튼

---

**2단계 — API 키 생성**

왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보** → 상단 **+ 사용자 인증 정보 만들기** → **API 키** 선택 → 생성된 키 복사

---

왼쪽 메뉴가 안 보이면 화면 왼쪽 상단 **≡ (햄버거 메뉴)** 클릭하면 나옵니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

여기서 어디로 가야되?

## 답변

현재 화면에서 순서대로 따라오세요.

**1단계 — Custom Search API 활성화**

왼쪽 메뉴 (≡) → **API 및 서비스** → **라이브러리** → 검색창에 `Custom Search API` 입력 → 클릭 → **사용 설정** 버튼

---

**2단계 — API 키 생성**

왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보** → 상단 **+ 사용자 인증 정보 만들기** → **API 키** 선택 → 생성된 키 복사

---

왼쪽 메뉴가 안 보이면 화면 왼쪽 상단 **≡ (햄버거 메뉴)** 클릭하면 나옵니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

여기서 어디로 가야되?

## 답변

현재 화면에서 순서대로 따라오세요.

**1단계 — Custom Search API 활성화**

왼쪽 메뉴 (≡) → **API 및 서비스** → **라이브러리** → 검색창에 `Custom Search API` 입력 → 클릭 → **사용 설정** 버튼

---

**2단계 — API 키 생성**

왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보** → 상단 **+ 사용자 인증 정보 만들기** → **API 키** 선택 → 생성된 키 복사

---

왼쪽 메뉴가 안 보이면 화면 왼쪽 상단 **≡ (햄버거 메뉴)** 클릭하면 나옵니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

여기서 어디로 가야되?

## 답변

현재 화면에서 순서대로 따라오세요.

**1단계 — Custom Search API 활성화**

왼쪽 메뉴 (≡) → **API 및 서비스** → **라이브러리** → 검색창에 `Custom Search API` 입력 → 클릭 → **사용 설정** 버튼

---

**2단계 — API 키 생성**

왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보** → 상단 **+ 사용자 인증 정보 만들기** → **API 키** 선택 → 생성된 키 복사

---

왼쪽 메뉴가 안 보이면 화면 왼쪽 상단 **≡ (햄버거 메뉴)** 클릭하면 나옵니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

여기서 어디로 가야되?

## 답변

현재 화면에서 순서대로 따라오세요.

**1단계 — Custom Search API 활성화**

왼쪽 메뉴 (≡) → **API 및 서비스** → **라이브러리** → 검색창에 `Custom Search API` 입력 → 클릭 → **사용 설정** 버튼

---

**2단계 — API 키 생성**

왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보** → 상단 **+ 사용자 인증 정보 만들기** → **API 키** 선택 → 생성된 키 복사

---

왼쪽 메뉴가 안 보이면 화면 왼쪽 상단 **≡ (햄버거 메뉴)** 클릭하면 나옵니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

여기서 어디로 가야되?

## 답변

현재 화면에서 순서대로 따라오세요.

**1단계 — Custom Search API 활성화**

왼쪽 메뉴 (≡) → **API 및 서비스** → **라이브러리** → 검색창에 `Custom Search API` 입력 → 클릭 → **사용 설정** 버튼

---

**2단계 — API 키 생성**

왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보** → 상단 **+ 사용자 인증 정보 만들기** → **API 키** 선택 → 생성된 키 복사

---

왼쪽 메뉴가 안 보이면 화면 왼쪽 상단 **≡ (햄버거 메뉴)** 클릭하면 나옵니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

여기서 어디로 가야되?

## 답변

현재 화면에서 순서대로 따라오세요.

**1단계 — Custom Search API 활성화**

왼쪽 메뉴 (≡) → **API 및 서비스** → **라이브러리** → 검색창에 `Custom Search API` 입력 → 클릭 → **사용 설정** 버튼

---

**2단계 — API 키 생성**

왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보** → 상단 **+ 사용자 인증 정보 만들기** → **API 키** 선택 → 생성된 키 복사

---

왼쪽 메뉴가 안 보이면 화면 왼쪽 상단 **≡ (햄버거 메뉴)** 클릭하면 나옵니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

여기서 어디로 가야되?

## 답변

현재 화면에서 순서대로 따라오세요.

**1단계 — Custom Search API 활성화**

왼쪽 메뉴 (≡) → **API 및 서비스** → **라이브러리** → 검색창에 `Custom Search API` 입력 → 클릭 → **사용 설정** 버튼

---

**2단계 — API 키 생성**

왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보** → 상단 **+ 사용자 인증 정보 만들기** → **API 키** 선택 → 생성된 키 복사

---

왼쪽 메뉴가 안 보이면 화면 왼쪽 상단 **≡ (햄버거 메뉴)** 클릭하면 나옵니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

여기서 어디로 가야되?

## 답변

현재 화면에서 순서대로 따라오세요.

**1단계 — Custom Search API 활성화**

왼쪽 메뉴 (≡) → **API 및 서비스** → **라이브러리** → 검색창에 `Custom Search API` 입력 → 클릭 → **사용 설정** 버튼

---

**2단계 — API 키 생성**

왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보** → 상단 **+ 사용자 인증 정보 만들기** → **API 키** 선택 → 생성된 키 복사

---

왼쪽 메뉴가 안 보이면 화면 왼쪽 상단 **≡ (햄버거 메뉴)** 클릭하면 나옵니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

여기서 어디로 가야되?

## 답변

현재 화면에서 순서대로 따라오세요.

**1단계 — Custom Search API 활성화**

왼쪽 메뉴 (≡) → **API 및 서비스** → **라이브러리** → 검색창에 `Custom Search API` 입력 → 클릭 → **사용 설정** 버튼

---

**2단계 — API 키 생성**

왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보** → 상단 **+ 사용자 인증 정보 만들기** → **API 키** 선택 → 생성된 키 복사

---

왼쪽 메뉴가 안 보이면 화면 왼쪽 상단 **≡ (햄버거 메뉴)** 클릭하면 나옵니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

여기서 어디로 가야되?

## 답변

현재 화면에서 순서대로 따라오세요.

**1단계 — Custom Search API 활성화**

왼쪽 메뉴 (≡) → **API 및 서비스** → **라이브러리** → 검색창에 `Custom Search API` 입력 → 클릭 → **사용 설정** 버튼

---

**2단계 — API 키 생성**

왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보** → 상단 **+ 사용자 인증 정보 만들기** → **API 키** 선택 → 생성된 키 복사

---

왼쪽 메뉴가 안 보이면 화면 왼쪽 상단 **≡ (햄버거 메뉴)** 클릭하면 나옵니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

여기서 어디로 가야되?

## 답변

현재 화면에서 순서대로 따라오세요.

**1단계 — Custom Search API 활성화**

왼쪽 메뉴 (≡) → **API 및 서비스** → **라이브러리** → 검색창에 `Custom Search API` 입력 → 클릭 → **사용 설정** 버튼

---

**2단계 — API 키 생성**

왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보** → 상단 **+ 사용자 인증 정보 만들기** → **API 키** 선택 → 생성된 키 복사

---

왼쪽 메뉴가 안 보이면 화면 왼쪽 상단 **≡ (햄버거 메뉴)** 클릭하면 나옵니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

여기서 어디로 가야되?

## 답변

현재 화면에서 순서대로 따라오세요.

**1단계 — Custom Search API 활성화**

왼쪽 메뉴 (≡) → **API 및 서비스** → **라이브러리** → 검색창에 `Custom Search API` 입력 → 클릭 → **사용 설정** 버튼

---

**2단계 — API 키 생성**

왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보** → 상단 **+ 사용자 인증 정보 만들기** → **API 키** 선택 → 생성된 키 복사

---

왼쪽 메뉴가 안 보이면 화면 왼쪽 상단 **≡ (햄버거 메뉴)** 클릭하면 나옵니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

여기서 어디로 가야되?

## 답변

현재 화면에서 순서대로 따라오세요.

**1단계 — Custom Search API 활성화**

왼쪽 메뉴 (≡) → **API 및 서비스** → **라이브러리** → 검색창에 `Custom Search API` 입력 → 클릭 → **사용 설정** 버튼

---

**2단계 — API 키 생성**

왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보** → 상단 **+ 사용자 인증 정보 만들기** → **API 키** 선택 → 생성된 키 복사

---

왼쪽 메뉴가 안 보이면 화면 왼쪽 상단 **≡ (햄버거 메뉴)** 클릭하면 나옵니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

여기서 어디로 가야되?

## 답변

현재 화면에서 순서대로 따라오세요.

**1단계 — Custom Search API 활성화**

왼쪽 메뉴 (≡) → **API 및 서비스** → **라이브러리** → 검색창에 `Custom Search API` 입력 → 클릭 → **사용 설정** 버튼

---

**2단계 — API 키 생성**

왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보** → 상단 **+ 사용자 인증 정보 만들기** → **API 키** 선택 → 생성된 키 복사

---

왼쪽 메뉴가 안 보이면 화면 왼쪽 상단 **≡ (햄버거 메뉴)** 클릭하면 나옵니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

여기서 어디로 가야되?

## 답변

현재 화면에서 순서대로 따라오세요.

**1단계 — Custom Search API 활성화**

왼쪽 메뉴 (≡) → **API 및 서비스** → **라이브러리** → 검색창에 `Custom Search API` 입력 → 클릭 → **사용 설정** 버튼

---

**2단계 — API 키 생성**

왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보** → 상단 **+ 사용자 인증 정보 만들기** → **API 키** 선택 → 생성된 키 복사

---

왼쪽 메뉴가 안 보이면 화면 왼쪽 상단 **≡ (햄버거 메뉴)** 클릭하면 나옵니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

여기서 어디로 가야되?

## 답변

현재 화면에서 순서대로 따라오세요.

**1단계 — Custom Search API 활성화**

왼쪽 메뉴 (≡) → **API 및 서비스** → **라이브러리** → 검색창에 `Custom Search API` 입력 → 클릭 → **사용 설정** 버튼

---

**2단계 — API 키 생성**

왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보** → 상단 **+ 사용자 인증 정보 만들기** → **API 키** 선택 → 생성된 키 복사

---

왼쪽 메뉴가 안 보이면 화면 왼쪽 상단 **≡ (햄버거 메뉴)** 클릭하면 나옵니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

여기서 어디로 가야되?

## 답변

현재 화면에서 순서대로 따라오세요.

**1단계 — Custom Search API 활성화**

왼쪽 메뉴 (≡) → **API 및 서비스** → **라이브러리** → 검색창에 `Custom Search API` 입력 → 클릭 → **사용 설정** 버튼

---

**2단계 — API 키 생성**

왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보** → 상단 **+ 사용자 인증 정보 만들기** → **API 키** 선택 → 생성된 키 복사

---

왼쪽 메뉴가 안 보이면 화면 왼쪽 상단 **≡ (햄버거 메뉴)** 클릭하면 나옵니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

여기서 어디로 가야되?

## 답변

현재 화면에서 순서대로 따라오세요.

**1단계 — Custom Search API 활성화**

왼쪽 메뉴 (≡) → **API 및 서비스** → **라이브러리** → 검색창에 `Custom Search API` 입력 → 클릭 → **사용 설정** 버튼

---

**2단계 — API 키 생성**

왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보** → 상단 **+ 사용자 인증 정보 만들기** → **API 키** 선택 → 생성된 키 복사

---

왼쪽 메뉴가 안 보이면 화면 왼쪽 상단 **≡ (햄버거 메뉴)** 클릭하면 나옵니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

여기서 어디로 가야되?

## 답변

현재 화면에서 순서대로 따라오세요.

**1단계 — Custom Search API 활성화**

왼쪽 메뉴 (≡) → **API 및 서비스** → **라이브러리** → 검색창에 `Custom Search API` 입력 → 클릭 → **사용 설정** 버튼

---

**2단계 — API 키 생성**

왼쪽 메뉴 → **API 및 서비스** → **사용자 인증 정보** → 상단 **+ 사용자 인증 정보 만들기** → **API 키** 선택 → 생성된 키 복사

---

왼쪽 메뉴가 안 보이면 화면 왼쪽 상단 **≡ (햄버거 메뉴)** 클릭하면 나옵니다.
