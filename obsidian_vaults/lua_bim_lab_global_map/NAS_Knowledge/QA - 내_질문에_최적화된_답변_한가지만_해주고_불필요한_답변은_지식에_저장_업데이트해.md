---
type: qa-note
category: Claude대화
domain: 기타
date: 2026-05-24
status: verified
tags:
  - QA
  - 기타
---

# QA - 내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 질문
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

## 관련 자료

- [[MOC - Q&A Knowledge Base]]

## 연결

- [[Global Knowledge Map]]

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.

---

## 질문 (추가 2026-05-24)
> **카테고리:** Claude대화 | **날짜:** 2026-05-24
> **출처:** Claude Code 세션

내 질문에 최적화된 답변 한가지만 해주고 불필요한 답변은 지식에 저장 업데이트해

## 답변

지금 흐름에서 문제:
- 답변에 `근거 문서`, `score`, `/ok`, `이 답변이 충분하면...` 같은 내부 정보가 섞여 나옴
- 웹 검색 결과 여러 개를 그대로 나열

`build_knowledge_answer`와 `build_more_research_answer` 두 곳을 수정합니다.
