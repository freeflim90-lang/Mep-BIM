---
type: qwen-product-draft
project: Model Quality Auditor
task_id: MQA-QWEN-001
status: generated
created: 2026-05-21
tags:
  - qwen
  - mqa
  - backend-draft
  - product-development
---

# MQA-QWEN-001 도메인 모델 및 데이터 계약 초안

## 작업 정보

| 항목 | 내용 |
|---|---|
| 산출물 | ModelIssue, AuditRule, AuditResult, ModelSnapshot 데이터 구조와 JSON 예시 |
| 범위 | Revit API 비의존 순수 도메인 모델. 실제 Document/Element 접근 금지. |
| 예상 출력 위치 | `backend/model_quality_auditor/domain_contracts.py` |
| Qwen 실행 | 성공 |
| 모델 | qwen2.5-coder:7b |
| 사유 | - |

## Qwen 초안

## MQA-QWEN-001: 도메인 모델 및 데이터 계약 초안

### Plan:
**목적**: Revit API 비의존 순수 도메인 모델을 정의하여 `Model Quality Auditor`의 품질진단 기능을 구현할 수 있도록 합니다. 이는 실제 Document/Element 접근 금지로, 백엔드 로직과 데이터 계약만 포함됩니다.

**입력**: 
- Revit API 비의존 도메인 모델 정의
- 진단 규칙 JSON 스키마
- 점수 계산 로직

**출력**: 
- `ModelIssue`, `AuditRule`, `AuditResult`, `ModelSnapshot` 데이터 구조와 JSON 예시

**제외 범위**: 
- Revit API 호출
- Document, Element, FilteredElementCollector 접근
- Transaction이 필요한 모델 변경
- 실제 Revit 버전 호환성
- Autodesk App Store 최종 제출 가능 여부

### Draft:
#### ModelIssue 데이터 구조
```python
from dataclasses import dataclass

@dataclass
class ModelIssue:
    issue_id: str
    description: str
    severity: str  # 'Pass', 'Minor', 'Major', 'Critical'
    category: str
```

#### AuditRule 데이터 구조
```python
from dataclasses import dataclass

@dataclass
class AuditRule:
    rule_id: str
    description: str
    category: str
    severity_threshold: str  # 'Pass', 'Minor', 'Major', 'Critical'
```

#### AuditResult 데이터 구조
```python
from dataclasses import dataclass, field
from typing import List

@dataclass
class AuditResult:
    model_name: str
    audit_date: str
    overall_score: float
    risk_level: str  # 'High', 'Medium', 'Low'
    issues: List[ModelIssue]
```

#### ModelSnapshot 데이터 구조
```python
from dataclasses import dataclass

@dataclass
class ModelSnapshot:
    model_name: str
    revit_version: str
    file_path: str
    creation_date: str
    last_modified_date: str
```

### Verification:
- 로컬 단위 테스트를 수행하여 각 데이터 구조의 유효성을 확인합니다.
  ```bash
  python -m unittest discover tests/model_quality_auditor
  ```
- JSON 예시를 생성하여 실제 사용 시의 데이터 포맷을 확인합니다.

### API 필요성 판단:
- 이 초안은 Revit API에 의존하지 않습니다. 모든 로직이 순수하게 도메인 모델과 데이터 계약으로 구성되어 있습니다.

### Next Draft Task:
1. **Task ID**: MQA-QWEN-002
2. **제목**: 진단 규칙 JSON 스키마 및 점수 계산 로직 구현
3. **산출물**: `audit_rules.json` 예시와 `score_calculation.py`
4. **예상 출력 위치**: backend/model_quality_auditor/rules_and_scores.py

## 연결

- [[Qwen Draft Index]]
- [[Qwen Development Boundary]]
- [[Revit API Test Gate Index]]
- [[Build Test Index]]
