---
type: qwen-product-draft
project: Model Quality Auditor
task_id: MQA-QWEN-002
status: generated
created: 2026-05-21
tags:
  - qwen
  - mqa
  - backend-draft
  - product-development
---

# MQA-QWEN-002 품질진단 룰 JSON 스키마 초안

## 작업 정보

| 항목 | 내용 |
|---|---|
| 산출물 | 룰 카테고리, 심각도, 검사 조건, 메시지, 수정 가이드 필드 설계 |
| 범위 | 명명, 파라미터, LOD, 링크, 좌표, 패밀리 품질을 표현하는 스키마. |
| 예상 출력 위치 | `backend/model_quality_auditor/rule_schema.json` |
| Qwen 실행 | 성공 |
| 모델 | qwen2.5-coder:7b |
| 사유 | - |

## Qwen 초안

### MQA-QWEN-002: 품질진단 룰 JSON 스키마 초안

#### 목적
Revit 모델의 품질을 진단하기 위한 규칙 JSON 스키마를 설계합니다. 이 스키마는 각 규칙의 카테고리, 심각도, 검사 조건, 메시지, 수정 가이드를 포함하며, 이를 통해 모델의 품질을 자동으로 평가할 수 있습니다.

#### 입력
- None

#### 출력
- `backend/model_quality_auditor/rule_schema.json`

#### 제외 범위
- Revit API 의존 코드
- 실제 Revit Document, Element, Transaction 접근 코드
- 고객 모델 데이터 외부 전송을 전제로 하지 않는 부분

### Draft: 구현 초안

```json
{
  "rule_category": {
    "type": "string",
    "description": "규칙의 카테고리 (e.g., '파라미터', '링크')"
  },
  "severity": {
    "type": "string",
    "enum": ["Pass", "Minor", "Major", "Critical"],
    "description": "규칙의 심각도"
  },
  "condition": {
    "type": "object",
    "properties": {
      "parameter_name": {
        "type": "string",
        "description": "파라미터 이름"
      },
      "expected_value": {
        "type": "any",
        "description": "기대되는 값"
      }
    },
    "required": ["parameter_name", "expected_value"]
  },
  "message": {
    "type": "string",
    "description": "규칙이 위반될 때 표시되는 메시지"
  },
  "fix_guide": {
    "type": "string",
    "description": "규칙을 수정하는 방법에 대한 가이드"
  }
}
```

### Verification: 로컬 검증 방법과 테스트 케이스

1. **JSON 스키마 유효성 확인**:
   - JSON 스키마를 사용하여 `rule_schema.json` 파일의 구조가 올바르게 구성되어 있는지 확인합니다.
   
2. **규칙 예시 테스트**:
   - 몇 가지 규칙 예시를 작성하고, 해당 규칙이 올바르게 평가되는지 확인합니다.

### API 필요성 판단

- 이 스키마는 순수 로직으로 구현 가능하며, Revit API 의존 코드는 필요하지 않습니다.
- 실제 모델 데이터에 대한 접근은 필요하지 않으며, 고객 모델 데이터 외부 전송을 전제로 하지 않는 부분입니다.

### Next Draft Task

1. **다음 미완료 Task 확인**:
   - `qwen_product_draft_state.json` 파일을 확인하여 다음 미완료 작업을 확인합니다.
   
2. **상품 패키지 문서와 개발 경계 읽기**:
   - `06_Qwen_Drafts/MQA-QWEN-*.md` 파일을 열어, 상품 패키지 문서와 개발 경계를 다시 확인합니다.

3. **Qwen 로컬 초안 작성**:
   - 다음 작업에 대한 초안을 작성합니다.
   
4. **Telegram 중간보고**:
   - 완료 작업, 기록 위치, 다음 작업을 Telegram으로 보고합니다.
   
5. **다음 Task 대기 또는 연속 실행**:
   - 다음 큐 작업으로 자연스럽게 이어가기 위해 필요한 입력 또는 선행 조건을 확인하고, 다음 작업을 준비합니다.

수동 실행:

```bash
source .dev-venv/bin/activate && LOCAL_CODER_ENABLED=true python scripts/qwen_product_draft_runner.py
```

Telegram 없이 검증:

```bash
source .dev-venv/bin/activate && LOCAL_CODER_ENABLED=true python scripts/qwen_product_draft_runner.py
```

## 연결

- [[Qwen Draft Index]]
- [[Qwen Development Boundary]]
- [[Revit API Test Gate Index]]
- [[Build Test Index]]
