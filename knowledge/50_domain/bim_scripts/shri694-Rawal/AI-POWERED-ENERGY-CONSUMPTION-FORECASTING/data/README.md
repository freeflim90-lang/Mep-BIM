# Data Folder

Place your dataset here as `energy.csv` if you want to use external data.

Expected minimum columns:
- `Datetime` (or `timestamp`, `date`, `time`)
- `Energy` (or `energy_kwh`, `consumption`, `load`)

If `data/energy.csv` is missing, the project auto-generates a realistic virtual dataset.

